version: 0.1

phases:
  install:
    commands:
      - echo Installing dependencies
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started
      - sudo npm install
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - '**/*'